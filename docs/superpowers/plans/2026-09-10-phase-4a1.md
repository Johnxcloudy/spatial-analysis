# Phase4A1 Implementation Plan

> Execute the authorized CART-01A stage in the current branch with explicit
> backend/frontend ownership, focused RED/GREEN and independent review.

**Goal:** save and restore explicit vector display styles with coherent legends.

**Architecture:** Python validates versioned layer-style specs in a separate
project table; React edits local drafts and compiles the same rules for map/legend.
Legacy styles remain active until adoption; source snapshots are never modified.

**Tech Stack:** existing React/TypeScript/OpenLayers, Python/SQLite, Tauri; no new dependencies.

## Global constraints

See docs/phase-4a1.md for exact limits. App0.7.0, protocol/schema7, spec1, worker5,
readiness1. Root owns shared/contracts.ts. No automatic categories from viewport,
no full-field scan or formal export. Manual categories ≤64, spec ≤32KiB.

## Task1: persistence and validation (backend)

Files: gis-engine/src/spatial_engine/cartography.py (new), projects.py, workspace.py;
tests/test_cartography.py (new), tests/test_projects.py as required for schema7.

- [ ] Write failing tests for NULL/text distinctions, invalid input/version/fields,
  duplicate typed values, budgets, stale revision, atomic rejection and legacy preservation.
- [ ] Implement validate_spec(value, dataset), returning canonical JSON-compatible
  spec matching shared/contracts.ts; no field scan or geometry read.
- [ ] Add schema7 vector_cartography table; preserve exact old map_layers, migrate
  all previous versions with the existing backup/transaction flow.
- [ ] Integrate layer.update/cartography and workspace reads; reject active legacy
  vector-style edits and kind-incompatible configs; null restores original style.
- [ ] Verify save/reopen, corruption, Save As and unchanged snapshots/analysis.
  Run: uv run --project gis-engine --frozen --group dev pytest gis-engine/tests/test_cartography.py gis-engine/tests/test_projects.py gis-engine/tests/test_workspace_tasks.py -q

## Task2: interactive style and legend (frontend)

Files: vector-style.ts, VectorStyleEditor.tsx/VectorLegend.tsx (new), LayerPanel.tsx,
VectorMap.tsx, styles.css and new focused tests. Consume agreed shared contract.

- [ ] Add failing focused tests for typed values/NULL/empty/other/constructor,
  palette provenance, matching map/legend colors and explicit draft application.
- [ ] Implement preset constructors and compiled style resolver; use Map/own-key
  checks, bound caches to configured categories, convert pt to CSS pixels explicitly.
- [ ] Provide manual value-type selector, category labels/colors, null/other,
  symbol settings, legend settings, Apply/Reset and restore-original controls.
- [ ] Integrate active spec property field and cached OL symbols; style-only changes
  must not change viewport request key. Show legend as configured symbols without counts.
- [ ] Run npm --prefix apps/desktop run test -- vector-style cartography;
  type-check via npm run check. Report exact test names/results, not assumed coverage.

## Task3: integration and acceptance (root)

Files: shared/contracts.ts/protocol.md, bridge.ts/use-workspace.ts if needed,
version/lock/strict version fixtures, scripts/verify-cartography.py and native harness.

- [ ] Update app/protocol/schema assertions and runtime metadata, keep worker protocol.
- [ ] Review both diffs against limits/compatibility; run relevant full regressions.
- [ ] Add source/frozen workflow acceptance: exact spec roundtrip, explicit restore,
  typed categories, rejected stale/invalid edits, Save As and input/statistics identity.
- [ ] Native real127 and reused100k/250k/500k fixtures: presets/legend/apply/reopen,
  responsive controls and no geometry refetch on color-only edits. Include late category
  manually with explicit other, without claiming automatic coverage or cold-cache results.
- [ ] Freeze/package/install/smoke/uninstall with final source manifest and actual hashes.
- [ ] Commit/push functional code, verify exact push and PR CI including public notice.
- [ ] Update README/AGENTS/TODO/roadmap, add verification and execution record, refresh
  ≤30-line handoff. Commit/push closeout and verify remote/clean/private-data exclusion.
