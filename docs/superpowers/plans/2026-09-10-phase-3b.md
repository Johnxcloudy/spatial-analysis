# Phase3B Implementation Plan

> Follow the authorized reliability milestone with explicit ownership, focused
> RED/GREEN regressions, independent review and factual stage closeout.

**Goal:** prevent query lifecycle leaks/late success and make CI failures and
native pagination latency attributable from recorded evidence.

**Architecture:** preserve all public contracts; parent monotonic deadline and
startup cleanup in QueryRunner, opt-in host timings, bounded harness/CI evidence.

**Tech Stack:** existing Python/pytest, Rust/Tokio, Tauri, Playwright and PowerShell.

## Global constraints

App0.6.2, protocol/schema6, worker5, readiness1. Existingfeat/phase-1a. Original
inputs and snapshots immutable; test/ and .artifacts ignored. Keep query2s/1GiB,
SQL1.5s and task900s/2GiB. No speculative cause for original PR failure/4s event.
Detailed scope and alternatives: docs/phase-3b.md.

## Task1: query lifecycle (analysis owner)

Files: gis-engine/src/spatial_engine/query_runner.py, resource_guard.py and
gis-engine/tests/test_query_runner_lifecycle.py, test_resource_guard_lifecycle.py.
ResourceGuard retirement race was independently reproduced and explicitly added.

- [x] Reproduce expired successful frame and Job construction failure; retain RED.
- [x] Check monotonic deadline when receiving frame; dispose/restart on expiration.
- [x] Make startup exception-safe for child/Job/guard/streams, including partial init.
- [x] Validate immediate/boundary/error responses, no leaked child, later recovery;
  run `uv run --project gis-engine --frozen --group dev pytest gis-engine/tests/test_query_runner_lifecycle.py`.
- [x] Reproduce stopped monitor late enforcement with explicit barriers; synchronize
  retirement and enforcement dispatch without holding the gate during sampling/wait.
- [x] Read existing runner/guard tests and run relevant regressions; root reviews.

## Task2: host and helper timings (root/UI owner)

Root owns apps/desktop/src-tauri/src/engine.rs, rpc_timing.rs, lib.rs and tests.
UI owns scripts/verify-pagination-ui.mjs; production frontend is read-only.

- [x] Add opt-in trace with request identity, queue/start/exchange/finish/total
  durations and sanitized error category; never serialize params or response.
- [x] Test privacy whitelist/default-off and timing record semantics; run Rust tests.
- [x] Record each harness native attempt, readiness wait and actual UI request
  timeline; source tagging is diagnostic-only via supported Tauri invoke headers.
- [x] Run one instrumented native repro on final data copies/500k fixture; correlate
  only unambiguous records, separately report UI feedback and total request latency.
- [x] Fix only additional reproducible defects; otherwise retain exact unknowns.

## Task3: CI failure evidence (performance owner)

Files: .github/workflows/ci.yml and scripts/summarize-pytest.py (or named equivalent);
focused script validation using synthetic XML only. No production data upload.

- [x] Keep pytest exit status and generate JUnit at .artifacts/pytest/junit.xml.
- [x] On success/failure produce bounded summary/escaped annotations including
  actual checkout, event, head/base information and failed test identity.
- [x] Preserve missing/malformed-report diagnostic; archive JUnit/summary even on failure.
- [x] Validate success/failure/special text/truncation/missing/corrupt input, then
  independently review and push CI diagnostics so new evidence runs in parallel.

## Task4: integration, distribution and closeout (root)

- [x] Bump app/locks to0.6.2, keep protocol/schema fixed, update acceptance versions.
- [x] Independent review and appropriate full regression, source/frozen queries and
  real-data preservation; no repeated passed suites without code change/reason.
- [x] Freeze/bundle, native pagination timings and local install/smoke/uninstall.
- [x] Exact functional Git/CI status with old failure preserved, update README/AGENTS/
  TODO/verification and append execution summary with short recovery handoff.
- [x] Verify functional remote and private-data exclusion; save handoff and invoke
  manual compaction only if exposed (currently unavailable).
  Documentation-only closeout uses a separate [skip ci] commit; verify HEAD/remote
  after push and save the actual result in .artifacts/phase3b-git-closeout.json.
