# Phase 3A Implementation Plan

> For agentic workers: use explicit file ownership and focused failing/passing
> regressions, then independent review and integration. Continue the authorized
> milestone; do not resume closed Phase 2 assignments.

**Goal:** improve bounded default paging and explicit recovery, and verify the
user's local GDB through complete import/export/project workflows.

**Architecture:** preserve immutable GPKG and existing RPC. Prove eligible numeric
ID/primary-key order before seeking; otherwise keep the original SQL semantics.
React retries only the current enabled query via its bounded latest-request hook.

**Tech Stack:** existing SQLite/GDAL/PyArrow, Python, React/TypeScript and Tauri.

## Global constraints

Application 0.6.1, public protocol/schema 6, worker 5, readiness 1. Existing
feat/phase-1a branch, no force push. Originals and registered snapshots stay
unchanged. Query SQL 1.5 seconds, worker 2 seconds/1 GiB; task 900 seconds/2 GiB.
No fabricated classification or geometry repair. test/ and .artifacts/ excluded
from Git. Detailed user-visible scope and proof/cache decision: docs/phase-3a.md.

## Task 1: verified default pages (performance owner)

Files: gis-engine/src/spatial_engine/vector_queries.py, optional focused
pagination.py helper, gis-engine/tests/test_vector_queries.py and new focused
pagination tests; scripts/verify-pagination.py. No vectors/tasks/schema edits.

- [ ] Write failing correctness/fast-path tests with canonical, gapped, permuted,
  noncanonical IDs, conflicting source field names, empty and old snapshot cases.
  Check complete ID/row sequence against the original CAST ORDER BY query.
- [ ] Implement only a proven default unfiltered path. Discover the real INTEGER
  PRIMARY KEY from schema; certify ID correspondence and contiguous range inside
  the shared SQL deadline. Do not trust dataset.featureCount as a new count.
- [ ] Run the original ordered SELECT when the proof does not hold or for
  filters/custom sorting. Keep invalid parameter and response budget handling.
- [ ] Use `uv run --project gis-engine --frozen --group dev pytest
  gis-engine/tests/test_vector_queries.py` and related tests; preserve RED/GREEN.
- [ ] Add repeatable fresh-process source/frozen paging measurement using
  synthetic 100000/250000/500000 snapshots; collect proof/query/total timings,
  expected first/last IDs and hash preservation, without claiming OS cold cache.

## Task 2: explicit current-page retry (UI owner)

Files: apps/desktop/src/use-queued-query.ts, use-vector-data.ts,
components/AttributeTable.tsx, VectorWorkspace.tsx, AnalysisWorkspace.tsx;
corresponding queued-query/vector/table/analysis tests, optional small
page-query-error.ts helper and styles.css. App.tsx version string only.

- [ ] First reproduce initial timeout displayed as empty and identical query
  unable to retry. Add tests for exact retry parameters, duplicate clicks and
  late success/error during key change/hide/unmount.
- [ ] Return identity-scoped error and guarded retry from useQueuedQuery; one
  retry creates a new AbortSignal for the current enabled request, no timeout loop.
- [ ] Show local attribute failure with retry; preserve paging history and query
  arguments. Reuse statistics refresh with a failed-page label and same offset.
- [ ] Keep ENGINE_* forwarding/reopen behavior; transient page failures stay local
  without clearing unrelated global errors. Change displayed version to0.6.1.
- [ ] Run `npm run check`, `npm test`, `npm run build`; retain existing rendering,
  paging, queue and cancellation assertions. Review real native paging separately.

## Task 3: real input acceptance (data owner)

Files: scripts/verify-real-vectors.py; local artifacts only otherwise.

- [ ] Expose --source, --layer, --output, optional --executable; reject existing
  output directory. Reuse persistent real RPC and task deadlines.
- [ ] Hash input collection before/after. Inspect/import complete data; compare
  source FID mapping, field values/types/nulls, CRS, geometries and quality counts
  through streamed independent reads without printing attribute values.
- [ ] Verify first/last page, sorting, bounded viewport/selection, GPKG export
  reread, project reopen/Save As, snapshot byte identity and original preservation.
- [ ] Run on the supplied local source; write actual blocked/passed checks and
  explicitly retain missing classification/study-boundary/geometry policy needs.

## Task 4: integration and delivery (root)

Files: versions/locks, native smoke version assertion, acceptance version checks,
CI integration as needed, README/AGENTS/TODO/phase docs/执行说明.md.

- [ ] Integrate0.6.1 and preserve all protocol/schema numbers; lock files checked.
- [ ] Independently review changes, run complete Python/frontend/Rust regression,
  relevant source RPC/old-schema/real-data checks and frozen build.
- [ ] Verify final frozen pagination and real import/export plus native UI and
  installation behavior. Reuse Phase2 analytic baselines only as history; run
  relevant current analysis regressions, do not relabel old full-scale evidence.
- [ ] Commit/push functional code, verify exact Windows CI, then append factual
  stage record, short handoff and next priorities; keep external acceptance pending.
- [ ] Check no user data/diagnostic outputs staged, verify remote/worktree, and
  invoke compaction if exposed; otherwise record that only handoff was saved.
