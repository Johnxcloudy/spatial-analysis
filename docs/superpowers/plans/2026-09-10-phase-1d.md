# Phase 1D Implementation Plan

> For agentic workers: use subagent-driven-development for bounded work with explicit ownership and review.

Goal: make the mixed GIS project portable and verify the next cartography architecture.

Architecture: Python owns consistent project snapshots, relocation identities and
task publication. React offers Save As and source management through versioned
Tauri RPC. CART-00 remains separate development probes.

Tech stack: existing Tauri/React/OpenLayers and Python/SQLite/GDAL/PROJ stack.

Status on 2026-09-10: all tasks complete. Source/frozen/native acceptance,
NSIS construction, local installation/uninstallation and Windows CI for the
pushed feature commit passed. Stage evidence and recovery handoff are saved.

## Global constraints

Use the existing feature branch, synthetic data, apply_patch, original input
preservation, exact public contracts in shared/contracts.ts, and the requirements
in docs/phase-1d.md. Do not overwrite existing projects or force-push. Application
0.5.0, RPC 5, schema 5, worker 4. Copy budget 32 GiB. Current GIS import/query
budgets remain in force. Sources and analysis values are immutable.

## Task 1: Python portability

Owner: portability worker. Files: gis-engine/src/spatial_engine and gis-engine/tests.

- [x] Add failing tests for schema-4 migration, a mixed project copy with unsaved
  form values, preserved original, snapshot identities and derived parent paths.
- [x] Implement schema-5 source locations/copy journals and cancellable worker
  operations, integrating exact RPC additions from docs/phase-1d.md.
- [x] Validate candidate fingerprints, target ownership, publication recovery,
  cancellation, escaped paths, missing/corrupt bytes and budgets.
- [x] Run `uv run --project gis-engine --frozen --group dev pytest gis-engine/tests`.
- [x] Report exact tests and decisions in `.artifacts/phase1d-portability-report.md`.

## Task 2: Desktop workflow

Owner: frontend worker. Files: apps/desktop/src only.

- [x] Add Save As dialog using current draft values and a new child directory.
  Track only the initiated task; open the copy after successful publication.
- [x] Add selected-dataset source status and relocation action; preserve original
  provenance display, show historical verification separately from presence.
- [x] Guard stale responses, recovery, dirty drafts and active tasks. Add labels
  for copy/relocation stages and ensure narrow windows remain usable.
- [x] Add focused state/UI regressions for success, cancellation, mismatch and
  failure without losing original project edits.
- [x] Run `npm run check`, `npm test`, `npm run build`.
- [x] Report in `.artifacts/phase1d-frontend-report.md`.

## Task 3: CART-00

Owner: cartography worker. Files assigned separately after read-only dependency
investigation. No shared/contracts.ts, existing app screens or engine RPC edits.

- [x] Write versioned draft spec with dataset versions, physical dimensions, CRS,
  styles, fonts, budgets, legend/NoData semantics and capability declarations.
- [x] Produce disposable OL/Python probes with the roadmap fixture, compare
  actual output dimensions, text, masks and vector/raster structure.
- [x] Record verified synthetic full-data and isolated frozen probe results,
  actual browser asset identities and remaining product-rendering limits without
  claiming production rendering or journal compliance.

## Task 4: Integration, delivery and closeout

Owner: root. Files: shared/contracts.ts, Rust, package metadata/locks, scripts,
CI, README/docs, TODO.md, AGENTS.md, 执行说明.md.

- [x] Publish shared additions; update versions, Rust allowlist and native smoke.
- [x] Add mixed-project RPC acceptance and native UI checks using real workers.
- [x] Review worker changes, resolve findings, run regression tests and
  frontend/Rust builds.
- [x] Build frozen engine; verify source/frozen workflows and native acceptance
  using new artifact directories. Record actual limitations.
- [x] Finish NSIS installer compression/build and verify the installed EXE,
  installation and uninstallation using a new temporary directory.
- [x] Commit/push, query corresponding Windows CI, record exact status.
- [x] Update scope/docs/TODO and stage summary with artifacts and handoff. Use a
  supported context-compaction capability if exposed; otherwise report absence.

Closeout: feature commit 41fe4b522eca506da9b0db99d13f60c10d12eb3e is pushed;
Windows CI 34426091682 succeeded. Evidence and remaining real-data/independent
Windows limitations are in docs/verification-phase-1d.md. No supported manual
conversation-compaction tool was exposed; the saved execution handoff is ready.
