# 执行说明

本文件记录阶段执行结果，并提供上下文恢复入口。详细设计、原始日志和验收报告保存在各自文档或产物目录；此处只保留继续工作所需的信息。历史测试是当时的执行结果，不代表本轮重新运行。

## 最新交接摘要

更新日期：2026-09-09。

- 产品目标：Windows 个人 GIS 工作站，优先完成土地利用、用地叠加与面积统计。首批验收继续使用合成样本。
- 用户长期要求：每完成一个阶段，先写阶段总结、更新本摘要，再执行可用的会话压缩；不可用时如实说明，以本文件作为压缩后的恢复依据。
- 已交付：Phase 0/1A/1B；Phase 1B 本机开发与分发验收、功能提交远程 CI 均完成，应用 0.3.0、协议 3、schema 3。
- 已有能力：四类普通矢量工作区；CSV/XLSX 独立表格快照、预览/属性/源行号、显式 X/Y/CRS 转点、错误行保留、GPKG 导出、取消与重开。
- 分工：React/OpenLayers 负责交互；Python 负责权威 GIS 与存储；Tauri 负责受控原生访问和进程生命周期。契约见 [shared/contracts.ts](shared/contracts.ts)。
- 数据约束：原始输入不可覆盖；Dataset 版本为快照 SHA-256；显示、来源和分析 CRS 分开；未知 CRS 受限，不用显示几何替代分析输入。
- 当前限制：矢量/地图上限沿用 1A；表格 100,000 行、2,000,000 单元格，来源列最多 254、转点最多 252；其余见 [Phase 1B 范围](docs/phase-1b.md)。
- 本轮验收：Python 127、前端 45、Rust 5 项通过；源码/冻结表格各 12 项、矢量各 10 项；真实旧版合成项目副本迁移、原生 UI、本机安装/卸载通过。
- Git：`feat/phase-1a` 的 Phase 1B 功能提交 `a8879f8156e7d3392f5883d0b7f7e4cc80b26d8f` 已推送，远程 SHA 已核对；不强制推送。
- CI：该功能提交的 [Windows CI 34324666936](https://github.com/Johnxcloudy/spatial-analysis/actions/runs/34324666936) 为 success；安装器构建、原生 EXE 冒烟测试及产物上传均通过。此结论对应功能提交，不泛指后续文档提交。
- 产物：0.3.0 NSIS 安装器，341,164,136 字节，SHA-256 见 [当前验收记录](docs/verification-phase-1b.md)。构建文件、合成项目和日志被 Git 忽略。
- 遗留验收：真实规划数据、ArcGIS 互操作、独立无开发环境的 Windows、离线/升级和大规模性能尚未完成；安装器未签名。
- 后续顺序：1C 栅格基础 → 1D 工作区整合/制图探针 → 2 用地分析 → 3 稳定性 → 4A/4B/4C 制图 → 5 Agent；详见 [TODO](TODO.md)。
- 制图与 Nature/Agent 已列入 [后续路线图](docs/cartography-roadmap.md)，目前是规划。程序执行版本化配置，Agent 提建议；制图不得改写分析结果。
- 当前状态：Phase 1B 收尾证据已写入文档，本次以文档提交保存；提交可用下方恢复命令定位。调试应用已关闭，重新检查确认仅 Vite 1420 保留；不自动启动 Phase 1C。
- 本机开发盘 I: 为 exFAT，保留两份 npm 锁文件，不依赖硬链接。环境命令见 [README](README.md)；恢复时重新检查服务/端口，不能复用历史 PID。
- 保留用户已有项目和其他未提交改动；测试仅写入新建的 `.artifacts` 或临时目录。当前没有需要用户补充数据才能继续合成样本开发的阻塞。
- 当前工具没有可直接触发的会话压缩接口；本文件提供精简交接，不表示实际对话已被压缩。实际压缩由宿主/系统执行。

## 阶段收尾流程

1. 核对本阶段已授权范围、验收条件和实际结果；未完成项明确写为待办或阻塞，不提前标记完成。
2. 在下方追加阶段记录，覆盖目标、完成内容/关键文件、设计决定、验证证据、问题与遗留风险、产物/Git 状态和下一步。修正旧记录时说明原因，不删除历史事实。
3. 同步 TODO、相关设计/验收文档；更新顶部交接摘要，通常控制在 30 行内。完整日志和大段讨论用链接引用。
4. 按当前授权执行 Git 提交/推送并核对结果。记录对应提交与 CI 的已知状态；仍在运行或未查询时明确注明。不能把本次说明文件尚未生成的提交 SHA 当作已知值。
5. 在压缩前记录未完成工作、最新用户约束、依赖和下一条可执行步骤。活动任务需要明确所有者、状态与恢复方法，不能在等待结果时误报整个阶段完成。
6. 若宿主提供可调用的压缩功能，在完成上述记录后调用；否则保留交接摘要并告知用户无法主动触发，不伪称压缩成功。
7. 向用户给出简短阶段总结和本文件链接。后续恢复先读最新用户指令、AGENTS.md、本文件顶部摘要与 TODO，再按需读取本阶段详细文档。

## 阶段记录

### 2026-09-09：Phase 0，基础与分发验证

- 目标与状态：建立 Windows 桌面、GIS 引擎、项目存储和分发基础；本机交付完成，独立无开发环境验收待完成。
- 完成内容：Tauri/React/Python 框架、受控 JSON-RPC、项目创建/保存/重开、GIS 诊断、投影预览、冻结引擎与 NSIS 安装器。
- 关键决定：保留标准 IO 协议并由宿主隐藏引擎窗口；分离 GDAL/PROJ 资源；在 exFAT 上使用独立 npm 安装和同卷资源替换。
- 历史验证：Python 23、前端 19、Rust 4 项通过；浏览器/原生界面、冻结引擎、本机安装与卸载通过。详细命令、产物及边界见 [Phase 0 验证记录](docs/verification.md)。
- Git：功能提交 `b774b87`。该阶段远程 CI 曾在深层 JSON 错误断言处失败，断言已在 Phase 1A 修正；不把历史本机通过写成该提交 CI 通过。
- 遗留与下一步：真实数据和独立机器验证保留；后续推进 Phase 1A 普通矢量工作区。

### 2026-09-09：Phase 1A，普通矢量与工作区

- 目标与状态：应用 0.2.0、协议 2、schema 2；完成本机交付及对应功能提交的远程 CI。
- 完成内容：四类格式真实驱动导入，字段/几何保全、来源身份、不可变快照、分页筛选/排序、地图与属性选择、图层样式/顺序、GPKG 导出及项目恢复。
- 关键文件：`gis-engine/src/spatial_engine/` 的 vectors/vector_queries/workspace/tasks/worker；`apps/desktop/src/` 工作区；`shared/contracts.ts`；`scripts/verify-vectors.py`。详细边界见 [实施范围](docs/phase-1a.md)。
- 关键决定：PyArrow 保留空值与大整数；任务子进程用同一冻结 EXE；发布日志协调文件与数据库；有界查询与正式数据分离。
- 已解决问题：Windows 进度文件替换竞态、发布后登记失败恢复、跨卷导出、取消清理、目标文件所有权保护、布尔筛选与 SHP 缺组件拒绝。
- 历史验证：`pytest` 87 项、`npm test` 30 项、Rust 4 项；类型/构建/格式检查通过；源码和冻结引擎各 10 项工作流、跨卷导出、原生 UI、安装与卸载通过。证据见 [Phase 1A 验证记录](docs/verification-phase-1a.md)。
- 产物：`apps/desktop/src-tauri/target/release/bundle/nsis/Spatial Analysis Desktop_0.2.0_x64-setup.exe`，340,596,036 字节；校验和在验收记录中。
- Git/CI：`feat/phase-1a`，功能提交 `d9922653a296506de75037d38b1ceeb53487464d` 已推送；[运行 34316781430](https://github.com/Johnxcloudy/spatial-analysis/actions/runs/34316781430) 为 success。
- 遗留与下一步：真实数据、完整 GDB 高级语义及独立机器等限制继续保留；进入 Phase 1B 后完成 CSV/XLSX 坐标表选择、字段/坐标验证和生成点数据。

### 2026-09-09：制图与 Agent 路线图更新

- 目标与状态：将用户两份新材料纳入后续开发；文档工作完成，未实现对应产品功能。
- 完成内容：建立 CART-00 至 CART-06，覆盖制图契约、主题、Layout、PNG/PDF、地形影像、栅格拉伸/分类、在线底图、高级格式与 Agent 顾问。
- 关键决定：先完成用地分析和稳定性，再交付基础成果出图，后加地形与 Agent；Nature 是有来源的可配置预设；正式导出不能复用截断的交互显示集合。
- 文档与验证：更新 [制图路线图](docs/cartography-roadmap.md)、[总计划](docs/plan-v2.md)、TODO、AGENTS 和 README；完成文档依赖/范围复核与 `git diff --check`。这是文档更新，没有重跑应用测试。
- Git：提交 `fa7b320e1e0d998f4b8f728ba090853baaa1c6d4` 已推送至 `feat/phase-1a`；本记录未核查该文档提交的远程 CI 结论。
- 遗留与下一步：渲染路径、出版规则和 Agent 集成在对应阶段实测选型；下一产品阶段仍为 Phase 1B。

### 2026-09-09：阶段总结与上下文交接流程

- 目标与状态：落实用户“每阶段总结、写入执行说明并压缩上下文”的长期要求；记录与恢复流程已建立。
- 完成内容：创建本文件，补录既有阶段；更新 AGENTS、README、TODO，固定收尾和恢复时的阅读顺序。
- 验证：核对历史阶段记录和文档路径，执行 `git diff --check`；本轮仅改文档，未重跑应用测试。
- 执行限制：当前未提供可调用的会话压缩接口，已保存精简交接摘要；没有将摘要落盘描述为真实会话已压缩。
- Git：随本轮文档提交保存，创建提交可通过 `git log --diff-filter=A -- 执行说明.md` 核对；不预填尚未生成的提交 SHA。
- 下一步：阶段结束后继续按本流程记录；当前不启动 Phase 1B。

### 2026-09-09：Phase 1B，坐标表与点数据

- 目标与状态：应用 0.3.0、协议 3、schema 3；本机功能和分发验收、功能提交 Git 推送与 CI 核查完成。用户已授权 Phase 1B 开发，继续使用合成样本。
- 完成内容：CSV/XLSX 解析选项与预览、独立非空间 GPKG、完整表头映射与 XLSX 类型/格式元数据、分页筛选/排序、源行号、显式坐标转点、错误记录与 GPKG 导出。
- 关键文件：`tables.py`、`vector_queries.py`、projects/workspace/tasks/worker；前端 ImportTable/GeneratePoints 与工作区；共享契约、Rust 白名单/原生验证、`scripts/verify-tables.py`、`verify-tables-ui.mjs`、`verify-migration.py`。
- 关键决定：表格与派生点是两个不可变数据集；X/Y/CRS 明确选择，错误坐标保留 NULL geometry；XLSX 公式不执行，主表规范化文本/NULL 与辅助元数据共同导出；正式面积分析仍未实现。
- 本轮验证：Python 127、前端 45、Rust 5 项通过；TypeScript、生产/调试构建、Rust 格式与 uv 锁检查通过；源码及冻结表格各 12 项工作流、四格式矢量各 10 项；旧版合成项目副本迁移保留 4 个数据集与图层/任务/快照。详见 [验收记录](docs/verification-phase-1b.md)。
- 原生与分发：桌面/窄视口截图、Canvas 非空/缩放/定位、表格和矢量流程、草稿与重开通过；冻结引擎基础验证、发布版 EXE、本机临时安装/卸载通过。所有输出为新建测试目录，原始项目保留。
- 修正：工作表列举独立于首张表可用性；稀疏 XLSX 补齐前检查矩阵预算；预览提前退出释放句柄；过期响应和旧地图定位请求不覆盖当前选择；UTF-8 BOM 文案匹配解析行为。
- 产物：`apps/desktop/src-tauri/target/release/bundle/nsis/Spatial Analysis Desktop_0.3.0_x64-setup.exe`，341,164,136 字节；SHA-256 `A0143408B44929052F232489432EBA7907814B4DB0BD465044582F4AF70E4802`。
- Git/CI：功能提交 `a8879f8156e7d3392f5883d0b7f7e4cc80b26d8f` 已推送至 `feat/phase-1a`，远程 SHA 一致；[Windows CI 34324666936](https://github.com/Johnxcloudy/spatial-analysis/actions/runs/34324666936) 已核对为 success，Windows job `102379065170` 的安装器、原生 EXE 及上传步骤通过。功能和验证脚本由独立工作者复核，剩余接口问题已修正。
- 收尾复核：重新读取源码/冻结工作流、迁移、UI、发布版与安装后报告，均为 `ok=true`；重算安装器 SHA-256 一致。只修改收尾文档，通过文档复核与 `git diff --check`，没有重复运行本机应用测试；提交使用 `[skip ci]` 跳过重复构建。本次文档提交可用 `git log -1 --format=fuller --grep="docs: close Phase 1B delivery"` 定位，恢复时用 `git status` 和 `git ls-remote origin refs/heads/feat/phase-1a` 再核对推送状态。
- 遗留：真实规划数据、ArcGIS 互操作、独立无开发环境/离线 Windows、升级和大规模性能待验收；安装器未签名。XLS/XLSM、完整工作簿格式与公式计算不在支持范围。
- 下一步：本阶段结束；下一产品阶段为 Phase 1C GeoTIFF 基础读取/显示，随后 1D、2；制图与 Agent 保持后续规划，待用户继续指令后实施。
- 上下文：已刷新本文件精简交接；没有可调用的手动压缩接口，不能将落盘摘要称为实际会话已压缩。

## 新阶段记录模板

```markdown
### 日期：阶段名称与版本

- 目标与状态：本阶段范围；完成/部分完成/阻塞。
- 完成内容与关键文件：用户可以完成的工作及实现入口。
- 关键决定：选型、行为约束和重要取舍。
- 验证：实际命令、结果、证据链接；未运行或未通过项单列。
- 问题与遗留：已解决故障、当前限制、阻塞原因与所需条件。
- 产物与 Git：文件位置/必要校验和、分支、已知提交、推送及对应 CI 状态。
- 下一步：按优先级列可执行任务，注明本阶段未授权的新范围。
```
